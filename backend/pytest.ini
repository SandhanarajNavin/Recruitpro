[pytest]
testpaths = tests
; The app is imported from the backend root rather than installed, so the root has
; to be importable. tests/conftest.py adds it too, for direct file invocations.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
